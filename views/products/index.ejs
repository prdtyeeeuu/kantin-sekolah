
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body class="bg-gray-100">
    <div class="container mx-auto mt-8">
        <h1 class="text-3xl font-bold text-center text-gray-800 mb-6"><%= title %></h1>
        <div class="flex justify-end mb-4">
            <a href="/products/add" class="bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded">Tambah Produk</a>
        </div>
        <div class="bg-white shadow-md rounded-lg overflow-hidden">
            <table class="min-w-full bg-white">
                <thead class="bg-gray-800 text-white">
                    <tr>
                        <th class="w-1/4 py-3 px-4 uppercase font-semibold text-sm">Nama</th>
                        <th class="w-1/4 py-3 px-4 uppercase font-semibold text-sm">Harga</th>
                        <th class="w-1/2 py-3 px-4 uppercase font-semibold text-sm">Deskripsi</th>
                        <th class="py-3 px-4 uppercase font-semibold text-sm">Aksi</th>
                    </tr>
                </thead>
                <tbody class="text-gray-700">
                    <% products.forEach(product => { %>
                        <tr>
                            <td class="w-1/4 py-3 px-4"><%= product.name %></td>
                            <td class="w-1/4 py-3 px-4">Rp <%= product.price %></td>
                            <td class="w-1/2 py-3 px-4"><%= product.description %></td>
                            <td class="py-3 px-4">
                                <a href="/products/edit/<%= product.id %>" class="text-blue-500 hover:text-blue-700">Edit</a>
                                <a href="/products/delete/<%= product.id %>" class="text-red-500 hover:text-red-700 ml-4" onclick="return confirm('Yakin ingin menghapus produk ini?')">Hapus</a>
                            </td>
                        </tr>
                    <% }); %>
                </tbody>
            </table>
        </div>
    </div>
</body>
</html>
